#ifndef _MATH_UTILS_H_
#define _MATH_UTILS_H_
#include "cglm/mat4.h"

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) < (b) ? (b) : (a))
#define PI 3.14159265359

// DELETEME: void perspective_lh(float, float, float, float, mat4);
// DELETEME: void lookat_lh(vec3, vec3, vec3, mat4, vec3, vec3);
#endif
