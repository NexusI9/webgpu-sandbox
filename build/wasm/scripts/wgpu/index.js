function main() {
  /*document.getElementById("call-button").addEventListener("click", () => {
    Module._setContext(
      window.innerWidth,
      window.innerHeight,
      window.devicePixelRatio,
    );
  });*/
}

window.onload = main;
